import 'package:tus_client/tus_client.dart';
import 'package:test/test.dart';

void main() {
  group('A group of tests', () {
    TusClient client;

    setUp(() {
      client = TusClient();
    });
  });
}
